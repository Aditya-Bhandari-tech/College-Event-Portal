# College Event Portal - Backend

## Scripts

### npm install
Installs all project dependencies from package.json

### npm start
Runs the server in production mode on port 5000

### npm run dev
Runs the server in development mode with auto-restart using nodemon


### npm init -y
 ### npm install express mongoose dotenv cors
 ###  npm install --save-dev nodemon
        "scripts": {
  "start": "node server.js",
  "dev": "nodemon server.js"
} change script like this!!

### npm install bcryptjs